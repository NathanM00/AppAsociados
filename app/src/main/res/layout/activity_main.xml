<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:layout_editor_absoluteY="81dp"
    android:background="#FAFAFB"
    >


    <!--

    Este es el boton para cerrar sesion para el que lo necesita
        <Button
            android:id="@+id/btn_salir"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="216dp"
            android:background="@drawable/btn_iniciar"
            android:elevation="10dp"
            android:text="cerrar sesion"
            android:textColor="#E8475B"
            app:layout_constraintStart_toStartOf="@+id/tv_menu"
            app:layout_constraintTop_toBottomOf="@+id/tv_menu" />

        -->

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/main_nav"
        android:id="@+id/fragment_contenedor"
        >


    </FrameLayout>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        tools:layout_editor_absoluteX="0dp"
        tools:layout_editor_absoluteY="453dp"
        app:menu="@menu/nav_items"
        android:background="#fff"
        app:itemIconTint="@color/newPrimaryColor"
        app:itemTextColor="@color/newPrimaryColor"
        android:id="@+id/main_nav"
        />


</RelativeLayout>